// This file is not used since we're using Java Spring Boot backend
// All database operations are handled by the backend API

// Dummy exports to prevent build errors in components that might still import from here
export const db = null;
export const schema = {};
